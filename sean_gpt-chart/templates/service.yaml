apiVersion: v1
kind: Service
metadata:
  name: {{ include "sean_gpt-chart.fullname" . }}
  labels:
    {{- include "sean_gpt-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sean_gpt-chart.selectorLabels" . | nindent 4 }}
